# Gender
#install.packages("questionr")
library(questionr)
## Para saber as classes de todas as variaveis
# a Função SAPPLY aplica a função class() em todas as colunas
# do banco de dados starswars de uma vez
sapply(starwars, class())
## Para saber as classes de todas as variaveis
# a Função SAPPLY aplica a função class() em todas as colunas
# do banco de dados starswars de uma vez
sapply(starwars, class)
data(starwars)
#install.packages("dplyr")
library(dplyr)
data(starwars)
class(starwars)
# banco formato tibble
starwars
## Para saber as classes de todas as variaveis
# a Função SAPPLY aplica a função class() em todas as colunas
# do banco de dados starswars de uma vez
sapply(starwars, class)
lapply(starwars, class)
lapply(starwars$name, class)
## Para saber as classes de todas as variaveis
# a Função SAPPLY aplica a função class() em todas as colunas
# do banco de dados starswars de uma vez
sapply(starwars$height, class)
# Carrega banco
iris = iris
freq(iris$Species) # tira frequencia da variavel do banco
